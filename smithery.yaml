name: hydra
description: "402-native paid work engine for agents. 40 tools: web extraction, search, HTML-to-Markdown, DNS/SSL/URL checks, text diff, hash, encode/decode, JSON/CSV conversion, Wikipedia, arXiv, SEC EDGAR, regulatory intelligence, prediction markets, FOMC data, oracle feeds. Pay-per-call in USDC on Base L2 via x402."
startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
  url: https://hydra-api-nlnj.onrender.com/mcp
